apiVersion: v1
kind: Service
metadata:
  name: {{ include "zouzoublique-api.fullname" . }}
  labels:
    {{- include "zouzoublique-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zouzoublique-api.selectorLabels" . | nindent 4 }}
